# Crewlyze
# Web Platform — FastAPI + Pure Web UI (no Streamlit)
# Install: pip install -r requirements.txt

# ── Core framework ────────────────────────────────────────────────────────────
crewai>=0.5.0
litellm>=1.0.0

# ── Web server ────────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart

# ── Data / ML ────────────────────────────────────────────────────────────────
pandas>=1.5
matplotlib>=3.6
seaborn>=0.12
# kaleido removed to avoid C++ build errors and freezes on new systems

# ── Utilities ────────────────────────────────────────────────────────────────
requests
huggingface_hub
python-dotenv
Pillow
reportlab

# ── Pinned for Windows compatibility ─────────────────────────────────────────
# regex has pre-built Windows wheels for Python 3.13
regex

# NOTE: streamlit has been removed — the app now runs on FastAPI + vanilla web UI
# NOTE: tiktoken removed — requires Rust; not needed
# NOTE: grpcio NOT listed — install only if needed: pip install grpcio --only-binary=:all:

duckdb>=0.9.0
requests>=2.31.0
scipy>=1.9.0
scikit-learn>=1.1.0
statsmodels>=0.14.0
openpyxl>=3.1.0
xlrd>=2.0.1
python-pptx>=0.6.21
